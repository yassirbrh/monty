#include "monty.h"
/**
 * nop - Function
 *
 * Description: Doesn't do anything.
 *
 * @stack: Pointer to the stack (useless but we're doing it for fun).
 * @line_number: Line number (added to be avoided).
 *
 * Return: No return because it's a void function.
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
